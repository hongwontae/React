1. 상태 끌어올리기의 기조는 상위 컴포넌트의 useState setFunc을 내려주고 하위 컴포넌트에서 호출하는 것이다.
=> 그러면 상위 컴포넌트가 트리거되어 재평가된다. (리렌더링의 조건 상위 컴포넌트가 리렌더링되면 하위 컴포넌트들도 리렌더링된다.)


2. input type이 숫자라도 JS로 가져올 떄 문자열로 가져온다. => + 넣으면 된다. or Number() 형변환
