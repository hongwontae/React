1. Strict Mode는 컴포넌트이다. => 사용하고 싶다면 react 라이브러리에서 import후 감싸야 한다.
Strict Mode는 컴포넌트 함수를 두 번 실행시킨다. => 오류를 찾기 위해서

*** 처음 렌더링 될 떄는 컴포넌트 외부도 실행되고 내부도 실행된다. but 리렌더링 (setFunc 같은 것들을 통해) 된다면 트리거되지 않는 한 외부는 실행되지 않는다.

