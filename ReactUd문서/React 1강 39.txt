React 1강 39

상위에서 하위로 데이터를 내려보낸다.
컴포넌트가 export하고 받을 객체가 import 한다해도 구현하지 않으면 데이터가 나타나지 않는다.

데이터 전달에 관한 깊은 이해
1. export => import는 필수적인 로직이다.
2. 한 컴포넌트에서 데이터를 만든다. => 다른 컴포넌트로 보내고 싶다. => <컴포넌트 속성값/>준다.

41. 컴포지션
props.children => 래퍼 컴포넌트를 생성
children => 태그와 태그 사이의 모든 내용을 표시하기 위해 사용되는 특수한 props
그냥 layout