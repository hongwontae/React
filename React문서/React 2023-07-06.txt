React jar

Board를 누르면 하위 메뉴를 띄우고 

code 스프린팅

board list로 들어오는 방식은 두 가지가 있다.
눌러서 들어오는 것, 사용자가 링크타고 들어오는 것

모든 기준을 라우터로 잡아야 한다.
page,size,keyword,type을 가져와야 한다.
이동처리도 컴포넌트가 처리한다.

브라우저 쿼리스트링을 가져오는 기능
링크로 동적으로 작동하는 기능
이 두가지가 필요하다.
BoardAPI가 필요하다.

real 개발은 url를 기준으로 해야 한다.
useSearchParams

axios로 dto를 보낸다.
 우리가 보낼 때 json데이터를 보내는 것은 아니다. 쿼리스트링으로 보낸다.
react에서 쿼리obj로 받으면 객체를 받는 것이다. 
boot와 react의 데이터 형식이 맞지 않는다.
object 타입으로 받아도 제대로 받아줄 react 키워드가 있다.

use가 붙는 애들은 컴포넌트에서만 사용한다.
usex 함수는 자체적인 함수일 뿐이다.

항상 싱크를 맞춰줘야 한다. 번호를 누른다는 것은 url를 바꿔주는 것이다.

쿼리 obj는 거르고 page값만 파라미터로 들어오는 값으로 바꿔주는 것이다.










